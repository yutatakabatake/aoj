#include <stdio.h>
#include <math.h>

int main()
{
    int W, H, x, y, r;

    scanf("%d %d %d %d %d", &W, &H, &x, &y, &r);

    if (x < W && y < H)
    {
        /* code */
    }

    // printf("%d %d %d %d %d\n", W, H, x, y, r);

    return 0;
}